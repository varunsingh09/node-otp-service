const message = (otp) =>{
    return `Dear User,\n`
    + `${otp} is your otp for Incampus. Please enter the OTP to verify your phone number.\n`
    + `Regards\n`
    + `Varun Singh`
}

module.exports= message;